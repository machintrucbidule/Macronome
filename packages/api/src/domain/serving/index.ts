// Serving domain — pure quantity→grams + macro-snapshot helpers (00-conventions.md).
export {
  resolveServedGrams,
  snapshotMacros,
  type ServingUnit,
  type MacroPer100g,
  type MacroSnapshot,
  type ServedGramsInput,
} from './serving.js';
