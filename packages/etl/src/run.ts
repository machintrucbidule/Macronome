// One-shot Excel → DB migration entry point.
// Stub only — SUPERSEDED: the workbook is migrated through the in-app Settings → import
// (the IMP-1 export/import envelope), using an extract prepared per spec/logic/migration-etl.md.
function main(): void {
  console.log('Macronome ETL — superseded; migrate via the in-app Settings → import.');
}

main();
