/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Version of the running bundle, injected at build time (vite.config.ts `define`, B-286). */
declare const __APP_VERSION__: string;
